import { react } from "react";

export default function AssignedTaskList() {
    return (
        <div className="flex flex-col justify-center items-center h-full w-[50%] font-[family-name:var(--font-geist-sans)] bg-green-100">
            <h1>Assigned</h1>
        </div>
    );
}