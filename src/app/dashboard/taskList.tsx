import { react } from "react";

export default function TaskList() {
    return (
        <div className="flex flex-col justify-center items-center h-full w-[50%] font-[family-name:var(--font-geist-sans)] bg-blue-100">
            <h1>Task List</h1>
        </div>
    );
}